'use strict';

// A list of all the modules loaded dynamically via variable strings,
// in order to help my dependency guessing tool discover and add them.

require('esm'); // used only in the bash script
require('perish');
